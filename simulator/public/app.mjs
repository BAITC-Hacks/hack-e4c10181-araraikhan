import {districts,indicators,measures,areas,baseline,example,validate,simulate,explain,improvements} from './engine.mjs';
const $=id=>document.getElementById(id);
const fmt=x=>x.toLocaleString('ru-RU',{maximumFractionDigits:2,minimumFractionDigits:2});
const short=x=>x.toLocaleString('ru-RU',{maximumFractionDigits:3});
let selections=[],filter=-1,revision=0,aiAvailable=false,busy=false;
const targets=Object.fromEntries(measures.filter(m=>m.scope==='district').map(m=>[m.id,'nura']));
function render(){
 const r=simulate(selections,{partial:true});
 $('filters').innerHTML=['Все',...areas].map((a,i)=>`<button class="filter" data-filter="${i-1}" aria-pressed="${filter===i-1}">${a}</button>`).join('');
 $('catalog').innerHTML=measures.filter(m=>filter===-1||m.area===filter).map(m=>{
  const selected=selections.some(s=>s.id===m.id),candidate={id:m.id,...(m.scope==='district'?{district:targets[m.id]}:{})};
  const errors=selected?[]:validate([...selections,candidate],{partial:true});
  return `<article class="measure ${selected?'chosen':''}"><div class="measure-top"><span>${areas[m.area]}</span><span class="measure-id">${m.id}</span></div><h3>${m.name}</h3><div class="measure-meta"><span>${m.scope==='city'?'Весь город':'Один район'}</span><span>Лаг ${m.lag} кв. · ${short((8-m.lag)/8*100)}% эффекта</span></div><div class="effects">${Object.entries(m.effects).map(([k,v])=>`<span class="effect ${v<0?'negative':''}" title="${indicators.find(i=>i[0]===k)[1]}: полный эффект ${v>0?'+':''}${v}">${k} ${v>0?'+':''}${short(v*(8-m.lag)/8)}</span>`).join('')}</div>${m.scope==='district'?`<label class="scope-label" for="target-${m.id}">Район реализации</label><select id="target-${m.id}" data-target="${m.id}" ${selected?'disabled':''}>${districts.map(d=>`<option value="${d.id}" ${targets[m.id]===d.id?'selected':''}>${d.name}</option>`).join('')}</select>`:'<span class="scope-label">Охват</span><div class="city-scope">Все пять районов</div>'}<div class="measure-bottom"><span class="price">${m.cost} <small>ед.</small></span><button class="add-button" data-add="${m.id}" ${errors.length?'disabled':''}>${selected?'✓ Выбрано':'+ Добавить'}</button></div>${errors.length?`<p class="blocked-reason">${errors[0]}</p>`:''}</article>`;
 }).join('');
 $('remaining').textContent=r.remaining;$('spent').textContent=`Использовано ${r.cost}`;$('count').textContent=`${selections.length} / 5 решений`;$('budget-fill').style.width=`${r.cost}%`;
 $('selected').innerHTML=selections.map((s,i)=>{const m=measures.find(m=>m.id===s.id);return `<div class="selection"><span>${i+1}.</span><div>${m.name}<small>${s.district?districts.find(d=>d.id===s.district).name:'Весь город'} · ${m.cost} ед.</small></div><button class="remove" data-remove="${s.id}" aria-label="Удалить ${m.name}">×</button></div>`;}).join('')+Array.from({length:5-selections.length},(_,i)=>`<div class="empty-slot">${selections.length+i+1}. Выберите инициативу</div>`).join('');
 $('status').textContent=r.complete?'Сценарий допустим. Можно разобрать результат.':`Осталось выбрать ${5-selections.length} мер. Итоговый Score появится после пятого решения.`;
 $('score-label').textContent=r.complete?'Итоговый Quality of Life Score':'Исходный Quality of Life Score';
 $('score').textContent=fmt(r.complete?r.score:baseline.score);
 $('score-delta').textContent=r.complete?`${r.score>=baseline.score?'+':''}${fmt(r.score-baseline.score)} к базе`:'из 100*';
 $('score-note').textContent=r.complete?`Слабейший район: ${r.weakest}. Критических показателей: ${r.critical.length}.`:'Промежуточные изменения показаны ниже. Итог пока не рассчитан.';
 $('analyze').disabled=!r.complete;
 $('city-caption').textContent=selections.length?(r.complete?'Результат через 8 кварталов':'Предварительные изменения · сценарий не завершён'):'Исходное состояние';
 $('districts').innerHTML=districts.map((d,i)=>`<article class="district ${r.scores[i]===r.minimum?'weakest':''}"><div class="district-name">${d.name}<span>${short(d.pop*100)}% жителей</span></div><div class="district-score">${fmt(r.scores[i])}${r.scores[i]!==baseline.scores[i]?`<small>${r.scores[i]>=baseline.scores[i]?'+':''}${fmt(r.scores[i]-baseline.scores[i])}</small>`:''}</div><div class="district-bar" role="meter" aria-label="Оценка района ${d.name}" aria-valuenow="${r.scores[i]}" aria-valuemin="0" aria-valuemax="100"><div style="width:${r.scores[i]}%"></div></div><p>${d.profile}</p></article>`).join('');
 $('matrix').innerHTML=`<caption class="fine">Показатели районов через 8 кварталов · значения 0–100 · в скобках изменение к базе</caption><thead><tr><th>Район</th>${indicators.map(([k,name,w])=>`<th><abbr title="${name}; вес ${short(w*100)}%">${k}</abbr></th>`).join('')}</tr></thead><tbody>${districts.map((d,i)=>`<tr><th>${d.name}</th>${r.rows[i].map((v,k)=>`<td class="${v<40?'cell-critical':v>d.values[k]?'cell-improved':''}" title="${indicators[k][1]}">${short(v)}${v!==d.values[k]?`<small>(${v>d.values[k]?'+':''}${short(v-d.values[k])})</small>`:''}</td>`).join('')}</tr>`).join('')}</tbody>`;
}
function update(next){const errors=validate(next,{partial:true});if(errors.length)throw Error(errors.join(' '));selections=next;selections.forEach(s=>{if(s.district)targets[s.id]=s.district;});revision++;busy=false;$('analysis-section').hidden=true;render();}
function analyze(){const r=simulate(selections);if(!r.valid)return;$('analysis-section').hidden=false;$('analysis-mode').textContent='Автоматический разбор по правилам';$('analysis-text').textContent=explain(r).join('\n\n');$('formula').innerHTML=[['Среднее по населению × 0,7',r.average*.7],['Слабейший район × 0,3',r.minimum*.3],['Штраф за критические показатели',-r.critical.length],['Итоговый Score',r.score]].map(([label,value])=>`<div class="formula-row"><span>${label}</span><strong>${fmt(value)}</strong></div>`).join('');
 const options=improvements(selections);$('suggestions').innerHTML=options.length?options.map((s,i)=>{const m=measures.find(m=>m.id===s.add.id),old=measures.find(m=>m.id===s.remove.id);return `<div class="suggestion">${old.id} ${old.name} → ${m.id} ${m.name} (${s.add.district?districts.find(d=>d.id===s.add.district).name:'весь город'})<br><strong>Score ${fmt(s.score)} · +${fmt(s.gain)}</strong> · бюджет ${s.cost}<br><button class="add-button" data-suggestion="${i}">Применить замену</button></div>`;}).join(''):'<p class="fine">Одна замена не улучшает этот сценарий. Это не доказывает глобальную оптимальность.</p>';
 $('suggestions').onclick=e=>{const b=e.target.closest('[data-suggestion]');if(!b)return;const s=options[Number(b.dataset.suggestion)];update(selections.map(x=>x.id===s.remove.id?s.add:x));analyze();};
 $('ai-button').disabled=busy||!aiAvailable;$('ai-status').textContent=aiAvailable?'AI объяснит проверенные сервером расчёты.':'OpenAI пока не подключён. Для локального запуска добавьте OPENAI_API_KEY в .env; для сайта — в секреты сервера.';
}
$('filters').onclick=e=>{const b=e.target.closest('[data-filter]');if(b){filter=Number(b.dataset.filter);render();}};
$('catalog').onchange=e=>{if(e.target.dataset.target){targets[e.target.dataset.target]=e.target.value;render();}};
$('catalog').onclick=e=>{const b=e.target.closest('[data-add]');if(!b)return;const id=b.dataset.add;if(selections.some(s=>s.id===id))update(selections.filter(s=>s.id!==id));else{const m=measures.find(m=>m.id===id);update([...selections,{id,...(m.scope==='district'?{district:targets[id]}:{})}]);}};
$('selected').onclick=e=>{const b=e.target.closest('[data-remove]');if(b)update(selections.filter(s=>s.id!==b.dataset.remove));};
$('example').onclick=()=>update(example.map(s=>({...s})));$('reset').onclick=()=>update([]);
$('analyze').onclick=()=>{analyze();$('analysis-section').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth',block:'start'});};
$('ai-button').onclick=async()=>{const current=revision;busy=true;$('ai-button').disabled=true;$('ai-status').textContent='AI анализирует сценарий…';try{const response=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({selections}),signal:AbortSignal.timeout(65000)});const data=await response.json();if(!response.ok)throw Error(data.error||'Не удалось получить ответ AI.');if(current!==revision)return;$('analysis-mode').textContent='Объяснение OpenAI · расчёты проверены сервером';$('analysis-text').textContent=data.text;$('ai-status').textContent='Числа рассчитаны моделью симулятора. Текст AI может требовать проверки.';}catch(e){if(current===revision)$('ai-status').textContent=e.name==='TimeoutError'?'AI не успел ответить. Попробуйте ещё раз.':e.message;}finally{if(current===revision){busy=false;$('ai-button').disabled=!aiAvailable;}}};
render();fetch('/api/config').then(r=>r.json()).then(c=>{aiAvailable=c.aiAvailable===true;if(!$('analysis-section').hidden)analyze();}).catch(()=>{});
if(document.modelContext?.registerTool){
 for(const tool of [
  {name:'read_city_scenario',description:'Read current city selections and calculated indicators.',inputSchema:{type:'object',properties:{},additionalProperties:false},annotations:{readOnlyHint:true},execute:()=>({selections,result:simulate(selections,{partial:true})})},
  {name:'set_city_scenario',description:'Replace the visible draft with up to five valid actions. Does not call AI.',inputSchema:{type:'object',properties:{selections:{type:'array',maxItems:5,items:{type:'object',properties:{id:{type:'string'},district:{type:'string'}},required:['id'],additionalProperties:false}}},required:['selections'],additionalProperties:false},annotations:{readOnlyHint:false},execute:input=>{update(input.selections);return {selections,result:simulate(selections,{partial:true})};}}
 ]){try{Promise.resolve(document.modelContext.registerTool(tool)).catch(()=>{});}catch{}}
}
